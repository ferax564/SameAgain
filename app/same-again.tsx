'use client';
import {useEffect,useRef,useState} from 'react';
import {ReceiptText,ChefHat,ListChecks,Compass,Heart,Users,ScanBarcode,Plus,ArrowUpRight,ArrowRight,Check,ChevronRight,MoreHorizontal,ShoppingBasket,Globe2,Search,Sun,Moon,WifiOff,CloudCheck,RefreshCw,Package,History,Copy,UserRound,CheckCheck,MapPin,ExternalLink,ArrowUp,Trash2,Repeat2,Settings2,X,Flag,Loader2,ArrowLeft,Share2,Monitor,Bookmark,LogOut,Download} from 'lucide-react';
import {SidebarProvider,Sidebar,SidebarContent} from '@/components/ui/sidebar';
import {Checkbox} from '@/components/ui/checkbox';
import {Switch} from '@/components/ui/switch';
import {Tabs,TabsList,TabsTrigger,TabsContent} from '@/components/ui/tabs';
import {DropdownMenu,DropdownMenuTrigger,DropdownMenuContent,DropdownMenuItem} from '@/components/ui/dropdown-menu';
import {AlertDialog,AlertDialogContent,AlertDialogHeader,AlertDialogTitle,AlertDialogDescription,AlertDialogFooter,AlertDialogCancel,AlertDialogAction} from '@/components/ui/alert-dialog';
import {Skeleton} from '@/components/ui/skeleton';
import {Toaster} from '@/components/ui/sonner';
import {toast} from 'sonner';
import {useHousehold,api} from '@/lib/use-household';
import {Product,RecordData,countries,countryTag,barcode,pack,conversion,canMerge,totals,rank,uid,repeatItem,substituteItem} from '@/lib/domain';
import {demoProducts} from '@/lib/demo';
import {Brand,Choice,Modal,Photo} from './ui';
import ReceiptScanner from './receipt-scanner';
import Scanner from './scanner';
import StoreHub from './store-hub';
import Meals from './meals';
import PhotoUpload from './photo-upload';
import {ProductNutrition,NutritionFields,IngredientReview} from './nutrition-panel';
import {retailers} from '@/lib/retailers';
import {matchesShopper,shopperTallies,claimAssignment} from '@/lib/shopping';
import {retailerScope} from '@/lib/catalogue-scope';
import {classifyRetailerProduct} from '@/lib/catalogue-quality';
import './same-again.css';
import './meals.css';
const substitutions={exact:'Exact product only',brand:'Same brand preferred',similar:'Similar alternatives accepted',ask:'Ask before substituting'};
const units={pack:'packs',piece:'pieces',kg:'kg',g:'g',l:'litres',ml:'ml'};
const navigation=[{id:'lists',label:'Lists',icon:ListChecks},{id:'discover',label:'Discover',icon:Compass},{id:'meals',label:'Meals',icon:ChefHat},{id:'favourites',label:'Favourites',icon:Heart},{id:'household',label:'Household',icon:Users}];
export default function SameAgain(){const s=useHousehold();const [view,setView]=useState('lists'),[selected,setSelected]=useState(''),[modal,setModal]=useState(''),[draft,setDraft]=useState<any>({}),[editing,setEditing]=useState<RecordData>(),[privateEditing,setPrivateEditing]=useState<RecordData>(),[quick,setQuick]=useState(''),[shopping,setShopping]=useState(false),[dark,setDark]=useState(false),[busy,setBusy]=useState(false),[query,setQuery]=useState(''),[searchCountry,setSearchCountry]=useState(''),[searchRetailer,setSearchRetailer]=useState(''),[dietFilter,setDietFilter]=useState(''),[results,setResults]=useState<Product[]>([]),[searched,setSearched]=useState(false),[searchError,setSearchError]=useState(''),[coverage,setCoverage]=useState<any>(),[scope,setScope]=useState<any>(),[product,setProduct]=useState<Product>(),[productLoading,setProductLoading]=useState(false),[productNotice,setProductNotice]=useState(''),[productStore,setProductStore]=useState(''),[photoBusy,setPhotoBusy]=useState(false),[destination,setDestination]=useState('FR'),[priority,setPriority]=useState('ingredients'),[compareItem,setCompareItem]=useState<RecordData>(),[matches,setMatches]=useState<any[]>([]),[matchReason,setMatchReason]=useState(''),[decisions,setDecisions]=useState<Record<string,any>>({}),[inviteUrl,setInviteUrl]=useState(''),[confirm,setConfirm]=useState<any>(null),[mergeQueue,setMergeQueue]=useState<any[]>([]),[location,setLocation]=useState(''),[awake,setAwake]=useState(false),[shopper,setShopper]=useState('all');const wake=useRef<any>(null),queryTimer=useRef<any>(null),matchSerial=useRef(0),productSerial=useRef(0),searchSerial=useRef(0);const household=s.household,hid=household?.id;const merge=mergeQueue[0];function setMerge(v:any){setMergeQueue(q=>v?[...q,v]:q.slice(1))}const lists=s.records.filter(r=>r.kind==='list'&&!r.data.archived),active=lists.find(l=>l.id===selected)||lists[0],items=s.records.filter(r=>r.kind==='item'&&r.data.list===active?.id).sort((a,b)=>(a.data.order||0)-(b.data.order||0)||a.created-b.created),outstanding=items.filter(r=>!r.data.done),done=items.filter(r=>r.data.done),visibleOutstanding=outstanding.filter(r=>matchesShopper(r,shopper,s.user?.id)),tallies=shopperTallies(items,s.members,s.user?.id),favourites=s.records.filter(r=>r.kind==='favourite'),trips=s.records.filter(r=>r.kind==='trip').sort((a,b)=>b.created-a.created),templates=s.records.filter(r=>r.kind==='template');const categories=household?.settings?.categories||['Fruit & vegetables','Dairy & alternatives','Bakery','Pantry','Frozen','Household','Other'];const listCats=active?.data.categoryOrder?.length?active.data.categoryOrder:categories;const currency=active?.data.currency||household?.settings?.currency||'EUR';const money=totals(items.map(i=>i.data),currency);const fmt=(n:number)=>new Intl.NumberFormat('en',{style:'currency',currency}).format(n);const memberName=(id:string)=>s.members.find(m=>m.user===id)?.name||'Former member';
useEffect(()=>{const d=localStorage.getItem('same-again:theme')==='dark';setDark(d);document.documentElement.classList.toggle('dark',d);const token=new URLSearchParams(window.location.search).get('invite');if(token){setDraft({token});setModal('join')}},[]);
useEffect(()=>{if(active&&!lists.some(l=>l.id===selected))setSelected(active.id)},[hid,active?.id,selected]);
useEffect(()=>{setShopper('all')},[hid,active?.id]);
const changeTheme=()=>{setDark(!dark);document.documentElement.classList.toggle('dark',!dark);localStorage.setItem('same-again:theme',dark?'light':'dark')};
async function act(action:string,data:any={},message='Saved'){if(s.pending&&['leave','deleteHousehold','deleteAccount','transfer'].includes(action)){toast.error('Sync pending changes before changing access.');return}if(s.demo){toast('This action needs your own household. Demo data stays separate.');return}setBusy(true);try{const r=await api(action,{household:hid,...data});toast.success(message);if(action!=='deleteAccount'){await s.boot();await s.refresh()}return r}catch(e:any){toast.error(e.message)}finally{setBusy(false)}}
function openItem(data:any={},old?:RecordData){if(!active){toast.error('Create a list first.');return}setEditing(old);setDraft({name:'',quantity:1,unit:'pack',pack:'',category:'Other',notes:'',substitution:'similar',list:active.id,order:Date.now(),...data});setModal('item')}
function add(data:any,to=active?.id,repeat=false){if(repeat)data=repeatItem(data);if(!to){toast.error('Create a shopping list first.');return}const d={quantity:1,unit:'pack',category:'Other',notes:'',store:lists.find(l=>l.id===to)?.data.store||'',substitution:'similar',...data,list:to,done:false,purchasedAt:null,purchasedBy:null,actualPrice:null,priceCurrency:lists.find(l=>l.id===to)?.data.currency||currency};const duplicate=s.currentRecords().find(r=>r.kind==='item'&&r.data.list===to&&!r.data.done&&canMerge(r.data,d));if(duplicate){setMerge({existing:duplicate,data:d});return}s.mutate('item',d);toast.success('Added to your list')}
function addReceipt(rows:any[],to:string){if(!lists.some(l=>l.id===to))throw new Error('Choose an active shopping list.');let count=0;for(const data of rows){const duplicate=s.currentRecords().some(r=>r.kind==='item'&&r.data.list===to&&!r.data.done&&r.data.receipt?.fingerprint===data.receipt.fingerprint&&r.data.receipt?.line===data.receipt.line&&r.data.receipt?.label===data.receipt.label&&r.data.quantity===data.quantity&&r.data.unit===data.unit&&r.data.pack===data.pack);if(duplicate)continue;const added=s.mutate('item',{...data,priceCurrency:lists.find(l=>l.id===to)?.data.currency||currency,order:Date.now()+count});if(!added)throw new Error('Sign in before adding receipt items.');count++}setSelected(to);setView('lists');return count}
function saveItem(e:React.FormEvent){e.preventDefault();if(!draft.name?.trim())return;if(editing){const oldList=lists.find(l=>l.id===editing.data.list),newList=lists.find(l=>l.id===draft.list);if(oldList?.data.currency!==newList?.data.currency||(draft.priceCurrency&&draft.priceCurrency!==(newList?.data.currency||currency))){draft.price=null;draft.actualPrice=null;draft.priceCurrency=newList?.data.currency;toast('Prices cleared because the destination list uses another currency.')}s.mutate('item',{...draft,priceCurrency:newList?.data.currency||currency},editing);toast.success('Item updated')}else add(draft,draft.list);setModal('')}
function toggle(item:RecordData){const updated=s.mutate('item',{...item.data,done:!item.data.done},item);toast(item.data.done?'Back on the list':'Added to your basket',{action:{label:'Undo',onClick:()=>{if(updated)s.mutate('item',item.data,updated)}},duration:6000})}
function remove(item:RecordData){const removed=s.mutate(item.kind,item.data,item,true);setModal('');toast('Item removed',{action:{label:'Undo',onClick:()=>{if(removed)s.mutate(item.kind,item.data,removed,false)}}})}
function saveFavourite(data:any){if(!data.product?.id)return toast.error('Save an exact or private product as a favourite.');if(favourites.some(f=>f.data.product?.id===data.product.id))return toast('Already in your favourites');s.mutate('favourite',{...data,list:undefined,done:undefined,price:null,actualPrice:null,purchasedBy:null,purchasedAt:null});toast.success('Saved to household favourites')}
async function openProduct(p:Product,store=''){
 setProductStore(store);
 if(s.demo)p=demoProducts.find(row=>row.id===p.id)||p;
 const serial=++productSerial.current;setProduct(p);setProductNotice('');setModal('product');
 if(p.source!=='Open Food Facts'||!p.barcode||!barcode(p.barcode).valid||s.demo){setProductLoading(false);return}
 setProductLoading(true);
 try{const r=await fetch('/api/catalogue?'+new URLSearchParams({barcode:p.barcode,details:'1',household:hid}),{signal:AbortSignal.timeout(25000)});const d=await r.json();if(serial!==productSerial.current)return;if(!r.ok)throw new Error(d.error||'Could not refresh this product.');
  if(!d.product){setProductNotice('This barcode is no longer found in the live catalogue. The saved record is shown below; check the package.');return}
  const refreshed=d.product as Product;const before=pack(p.pack),after=pack(refreshed.pack);const changed=p.pack&&refreshed.pack&&(before&&after?before.basis!==after.basis||before.amount!==after.amount:p.pack!==refreshed.pack);
  setProduct(refreshed);setResults(rows=>rows.map(row=>row.id===p.id?refreshed:row));
  setProductNotice([changed?`Pack-size discrepancy: the saved result says ${p.pack}; the current catalogue says ${refreshed.pack}. Check the actual package before adding.`:'',d.notice].filter(Boolean).join(' '));
 }catch(e:any){if(serial===productSerial.current)setProductNotice((e.message||'Details could not be refreshed.')+' Saved information remains visible. Retry or check the package.')}finally{if(serial===productSerial.current)setProductLoading(false)}
}
async function search(code?:string,overrides?:{retailer?:string;country?:string;label?:string}){
 setProductStore('');
 const serial=++searchSerial.current;clearTimeout(queryTimer.current);setBusy(true);setSearchError('');setSearched(true);setCoverage(undefined);setScope(undefined);
 const retailer=overrides?.retailer??searchRetailer;
 const country=overrides?.country??(retailer&&retailers[retailer]?retailers[retailer].country:searchCountry);
 const label=overrides?.label??dietFilter;
 try {
  if(!code && /^[\d\s-]{8,20}$/.test(query.trim())) code=query.trim();
  const filters:Record<string,string>={};
  if(!code){if(query.trim())filters.q=query.trim();if(country)filters.country=country;if(retailer)filters.retailer=retailer;if(label)filters.label=label}
  if(s.demo){
   setProductStore('');
   const params=new URLSearchParams(code?{barcode:code}:filters);
   const response=await fetch('/api/demo-catalogue?'+params,{signal:AbortSignal.timeout(15000)});
   const data=await response.json();if(!response.ok)throw new Error(data.error);
   if(serial!==searchSerial.current)return;
   if(code){const found=demoProducts.find(p=>p.barcode===barcode(code!).code)||data.product;if(found){setProduct(found);setProductLoading(false);setProductNotice(data.notice||'Saved demo catalogue record.');setModal('product')}else{setDraft({barcode:code,name:'',brand:'',pack:''});setPrivateEditing(undefined);setModal('private')}}
   else{setResults(data.products);setCoverage(data.coverage);setScope(data.scope||(retailer==='coop-ch'||retailer==='migros-ch'?retailerScope(retailer as 'coop-ch'|'migros-ch'):undefined));setSearchError(data.notice||'')}
  } else {
   const params=new URLSearchParams(code?{barcode:code,household:hid,details:'1'}:{...filters,household:hid});
   const r=await fetch('/api/catalogue?'+params,{signal:AbortSignal.timeout(25000)});if(r.status===401)throw Object.assign(new Error('Sign in again to look up products in your household.'),{status:401});const d=await r.json();if(!r.ok)throw Object.assign(new Error(d.error||'Product lookup failed. Please retry.'),{status:r.status});
   if(serial!==searchSerial.current)return;if(d.notice&&code)toast(d.notice,{duration:8000});if(code){if(d.product){setProduct(d.product);setProductLoading(false);setProductNotice(d.notice||'');setModal('product')}else{setDraft({barcode:code,name:'',brand:'',pack:''});setPrivateEditing(undefined);setModal('private')}}else {setResults(d.products);setCoverage(d.coverage);setScope(d.scope||(retailer==='coop-ch'||retailer==='migros-ch'?retailerScope(retailer as 'coop-ch'|'migros-ch'):undefined));if(d.notice)setSearchError(d.notice)}
  }
 }catch(e:any){if(serial===searchSerial.current){setSearchError(e.message);toast.error(e.message);if(code&&modal==='scanner')throw e}}finally{if(serial===searchSerial.current)setBusy(false)}
}
function browseRetailer(id:string){
 searchSerial.current++;setBusy(false);
 if(!id){setSearchRetailer('');setResults([]);setSearched(false);setSearchError('');setCoverage(undefined);setScope(undefined);return}
 setSearchRetailer(id);setSearchCountry(retailers[id].country);void search(undefined,{retailer:id,country:retailers[id].country});
}
function setSearch(v:string){searchSerial.current++;setBusy(false);setQuery(v);clearTimeout(queryTimer.current)}
async function findMatches(item:RecordData){const serial=++matchSerial.current;setCompareItem(item);setMatches([]);setMatchReason('');setBusy(true);try{if(s.demo){setMatches(rank(item.data.product,demoProducts,destination,household.settings.constraints,priority,item.data.substitution));setMatchReason('Example basket with real catalogue records. No candidate is invented when the sample catalogue has no credible match.')}else{const r=await fetch('/api/catalogue?'+new URLSearchParams({original:item.id,household:hid,country:destination,priority})),d=await r.json();if(!r.ok)throw new Error(d.error);if(serial!==matchSerial.current)return;setMatches(d.matches);setMatchReason(d.reason)}}catch(e:any){if(serial===matchSerial.current)setMatchReason(e.message)}finally{if(serial===matchSerial.current)setBusy(false)}}
function choose(item:RecordData,p:Product|null,reason:string,quantity?:number){setDecisions(d=>({...d,[item.id]:{version:item.version,product:p,reason,quantity:quantity||item.data.quantity}}));toast.success('Choice saved for the destination copy');setCompareItem(undefined)}
function translate(){if(!active)return;const originalItems=outstanding;if(originalItems.some(i=>!decisions[i.id]||decisions[i.id].version!==i.version))return toast.error('Review each current item before creating the destination copy. An item may have changed since your approval.');const copy=s.mutate('list',{name:active.data.name+' · '+countries[destination],country:destination,currency:destination==='US'?'USD':destination==='GB'?'GBP':destination==='CH'?'CHF':'EUR',originalList:active.id});if(!copy)return;for(const item of originalItems){const choice=decisions[item.id];s.mutate('item',{...substituteItem(item.data,choice.product),list:copy.id,quantity:choice.quantity,done:false,price:null,actualPrice:null,originalItem:item.id,replacementReason:choice.reason});if(choice.product&&choice.product.id!==item.data.product?.id)s.mutate('substitution',{original:item.data.product?.id,product:choice.product,country:destination,reason:choice.reason})}setSelected(copy.id);setModal('');setView('lists');toast.success('Destination list created. Your original is preserved.')}
function completeTrip(){if(s.pending)return toast.error('Sync pending changes before finishing this trip.');if(!done.length)return toast.error('Check off purchased items first.');s.mutate('trip',{name:active?.data.name,date:Date.now(),list:active?.id,currency,items:done.map(i=>({...i.data,originalItem:i.id,originalVersion:i.version}))});if(s.demo)for(const item of done)s.mutate('item',item.data,item,true);toast.success('Shopping trip saved. Remaining items stay on the list.')}
function nav(id:string){setView(id);setShopping(false);void keepAwake(false)}
async function keepAwake(value:boolean){try{if(value){if(!('wakeLock'in navigator))throw new Error('Keep screen awake is not supported by this browser.');wake.current=await (navigator as any).wakeLock.request('screen');wake.current.addEventListener('release',()=>setAwake(false));setAwake(true)}else{await wake.current?.release();setAwake(false)}}catch(e:any){toast.error(e.message)}}
function renderItem(item:RecordData){const d=item.data;const mine=d.assigned===s.user?.id;return <article key={item.id} className={'item-row '+(d.done?'completed':'')+(mine?' mine':'')+(!d.assigned&&!d.done?' unclaimed':'')}><Checkbox className="purchase-check" checked={!!d.done} aria-label={(d.done?'Uncheck ':'Buy ')+d.name} onCheckedChange={()=>toggle(item)}/><button className="item-main" onClick={()=>openItem(d,item)}><Photo product={{...d.product,name:d.name,image:d.image||d.product?.image}}/><div className="item-info"><div className="item-title">{d.name}{d.priority==='high'&&<Flag size={14} aria-label="High priority"/>}</div><span className="item-meta">{d.product?.brand?.split(',')[0]||d.category}{d.pack?' · '+d.pack:''}{d.assigned?' · '+memberName(d.assigned):''}</span>{d.notes&&<span className="item-note">{d.notes}</span>}{d.done&&<span className="item-meta">Bought by {memberName(d.purchasedBy)}</span>}</div><span className="quantity">{d.quantity}<small>{units[d.unit as keyof typeof units]||d.unit}</small></span></button><button className={'avatar small '+(d.assigned?'assigned':'unassigned')} aria-label={d.assigned?'Assigned to '+memberName(d.assigned)+'. Change shopper.':'Claim '+d.name} title={d.assigned?memberName(d.assigned):'I will get this'} onClick={()=>d.assigned&&d.assigned!==s.user?.id?openItem(d,item):s.mutate('item',{...d,assigned:claimAssignment(d.assigned,s.user?.id||'')},item)}>{d.assigned?memberName(d.assigned).slice(0,1):<UserRound size={16}/>}</button><DropdownMenu><DropdownMenuTrigger asChild><button className="iconbtn item-more" aria-label={'More actions for '+d.name}><MoreHorizontal size={18}/></button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onClick={()=>openItem(d,item)}>Edit or move item</DropdownMenuItem><DropdownMenuItem onClick={()=>saveFavourite(d)}>Save favourite</DropdownMenuItem><DropdownMenuItem onClick={()=>{setCompareItem(undefined);setDecisions({});setModal('translate');if(d.product)void findMatches(item)}}>Find abroad</DropdownMenuItem><DropdownMenuItem onClick={()=>s.mutate('item',{...d,order:Math.min(...items.map(x=>x.data.order||0))-1},item)}>Move to top</DropdownMenuItem><DropdownMenuItem onClick={()=>{setDraft({product:d.product?.id,name:d.name,store:d.store||'',date:new Date().toISOString().slice(0,10),price:''});setModal('observation')}}>Record where bought</DropdownMenuItem><DropdownMenuItem onClick={()=>remove(item)}>Delete</DropdownMenuItem></DropdownMenuContent></DropdownMenu></article>}
return <SidebarProvider><Toaster position="bottom-center" richColors/><Sidebar collapsible="none" className="app-side"><SidebarContent><Brand/><div className="household-picker"><span className="avatar"><Users size={20}/></span><div>{s.households.length>1&&!s.demo?<><small className="muted">SWITCH HOUSEHOLD</small><Choice label="Switch household" value={hid||''} onChange={s.switchHousehold} options={Object.fromEntries(s.households.map(h=>[h.id,h.name]))}/></>:<><small className="muted">YOUR HOUSEHOLD</small><strong>{household?.name||'Make room for everyone'}</strong></>}</div></div><nav aria-label="Main navigation">{navigation.map(n=><button key={n.id} className={'nav-item '+(view===n.id?'active':'')} onClick={()=>nav(n.id)}><n.icon size={21}/>{n.label}{n.id==='lists'&&lists.length>0&&<span className="nav-count">{lists.length}</span>}</button>)}</nav><div className="sidebar-lists"><div className="row between"><span className="eyebrow">YOUR LISTS</span><button className="iconbtn" aria-label="Create list" onClick={()=>{setDraft({name:'',currency:household?.settings.currency||'EUR',country:'IT',listName:'Weekly groceries'});setModal(household?'list':'create')}}><Plus size={18}/></button></div>{lists.map(l=><button className={'side-list '+(l.id===active?.id?'selected':'')} key={l.id} onClick={()=>{setSelected(l.id);nav('lists')}}><span className="list-mark"/><span>{l.data.name}</span></button>)}</div><div className="sidebar-bottom"><button className="nav-item" onClick={()=>setModal('history')}><History size={20}/> Shopping history</button><button className="nav-item" onClick={()=>setModal('about')}><Repeat2 size={20}/> About Same Again</button><div className="profile"><span className="avatar">{s.user?.name?.slice(0,1)||'S'}</span><span>{s.user?.name?.split(' ')[0]||'Welcome'}<small className="muted">{s.demo?'Demo experience':'Your everyday, together'}</small></span><button className="iconbtn" onClick={changeTheme} aria-label={dark?'Use light theme':'Use dark theme'}>{dark?<Sun size={18}/>:<Moon size={18}/>}</button></div></div></SidebarContent></Sidebar><div className={'app-body '+(shopping?'shopping-mode':'')}><header className="topbar"><div className="mobile-brand"><Brand/></div><div className="breadcrumb"><span>{household?.name||'Your household'}</span><ChevronRight size={14}/><strong>{navigation.find(n=>n.id===view)?.label}</strong></div><div className="row"><button className="sync" aria-live="polite" onClick={()=>s.flush()} aria-label={'Sync status: '+s.sync+'. Retry synchronisation'}>{s.sync==='Offline'?<WifiOff size={16}/>:s.sync==='Synced'?<CloudCheck size={16}/>:<RefreshCw size={16}/>}<span>{s.demo?'Demo on this device':s.sync}</span></button><button className="avatar" onClick={()=>nav('household')} aria-label="Open household settings">{s.user?.name?.slice(0,1)||<UserRound size={18}/>}</button></div></header>
{s.demo&&<div className="demo-banner"><span><strong>Demo household</strong><span className="desktop-only"> · Sample shopping activity, real catalogue photos.</span></span><button onClick={()=>{s.exitDemo();setSelected('')}}>Use my household <ArrowRight size={15}/></button></div>}
{s.rejected&&<div className="notice" role="alert"><strong>A change needs attention: {s.rejected.op.data.name||s.rejected.op.kind}</strong><p>{s.rejected.message}. Edit this record in the app to correct it, or discard this draft and any pending changes that depend on it.</p><button className="btn" onClick={()=>s.discardRejected()}>Discard rejected draft</button></div>}{s.error&&!s.rejected&&<div className="notice" role="alert">{s.error}<button className="link" onClick={()=>{s.setError('');void s.boot();void s.flush()}}> Retry</button></div>}
<main>{s.loading?<div className="stack"><Skeleton className="h-12 w-72"/><Skeleton className="h-64 w-full"/></div>:!household?<div className="onboarding"><div className="welcome-mark"><Repeat2 size={40}/></div><span className="eyebrow">A LITTLE LESS TO REMEMBER</span><h1>Your favourites.<br/>Everyone on the same list.</h1><p className="muted">From the weekly shop to somewhere new.<br/>Keep the things you love close.</p><div className="row wrap">{s.realUser?<><button className="btn primary" onClick={()=>{setDraft({name:'',country:'IT',currency:'EUR',language:'en',listName:'Weekly groceries'});setModal('create')}}><Plus size={18}/> Create a household</button><button className="btn" onClick={()=>{setDraft({token:''});setModal('join')}}>Join a household</button></>:<a href="/signin-with-chatgpt?return_to=%2F" target="_top" className="btn primary">Sign in with ChatGPT <ArrowRight size={18}/></a>}<button className="btn" onClick={s.enterDemo}>Explore the demo</button></div><div className="welcome-products">{demoProducts.slice(1,4).map(p=><div key={p.id}><Photo product={p} large/><span>{p.brand?.split(',')[0]}</span></div>)}</div><p className="fine">Personal households are private. Only invited members can access them.</p></div>:<>
{view==='lists'&&<><div className="page-heading"><div><span className="eyebrow">THE EVERYDAY, TAKEN CARE OF</span><h1>{active?.data.name||'Your shopping lists'}</h1><p className="muted">{active?<><strong>{outstanding.length} items</strong> to pick up{tallies.mine?` · ${tallies.mine} for you`:''}{active.data.store&&active.data.store!=='Any store'?' · '+active.data.store:' · A little teamwork goes a long way.'}</>:'Create your first list and add whatever comes to mind.'}</p></div><div className="row wrap heading-actions"><button className="btn" onClick={()=>setModal('receipt')} disabled={!active}><ReceiptText size={18}/> Scan receipt</button><button className={'btn '+(shopping?'primary':'')} onClick={()=>setShopping(!shopping)}><ShoppingBasket size={18}/>{shopping?'Exit shopping':'Shopping mode'}</button><button className="btn primary" onClick={()=>setModal('scanner')}><ScanBarcode size={19}/> Scan a product</button></div></div><div className="mobile-list-picker"><Choice label="Shopping list" value={active?.id||''} onChange={setSelected} options={Object.fromEntries(lists.map(l=>[l.id,l.data.name]))}/><button className="iconbtn" aria-label="New list" onClick={()=>{setDraft({name:'',currency});setModal('list')}}><Plus/></button></div>{shopping&&<div className="shopping-banner row between"><span><ShoppingBasket size={22}/><strong>{shopper==='all'?outstanding.length:visibleOutstanding.length} left to find</strong>{shopper!=='all'&&<small className="muted"> of {outstanding.length}</small>}</span><label className="row"><Switch checked={awake} onCheckedChange={keepAwake}/> Keep awake</label></div>}<div className="lists-layout"><section className="list-surface">{active&&<><form className="quick-add" onSubmit={e=>{e.preventDefault();if(quick.trim()){add({name:quick.trim(),unit:'piece',quantity:1});setQuick('')}}}><Plus size={22}/><input aria-label="Add an item" value={quick} onChange={e=>setQuick(e.target.value)} placeholder="What do we need?"/><button type="submit" className="btn primary">Add</button></form><div className="list-toolbar"><div className="row wrap"><span className="pill">{done.length} of {items.length} in basket</span><div className="avatar-group" aria-label="Household shoppers">{s.members.slice(0,5).map(m=><span className="avatar small" key={m.user} title={m.name}>{m.name[0]}</span>)}{s.members.length>5&&<span className="avatar small" title={`${s.members.length-5} more`}>+{s.members.length-5}</span>}</div></div><div className="list-menu"><DropdownMenu><DropdownMenuTrigger asChild><button className="iconbtn" aria-label="List options"><MoreHorizontal size={20}/></button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onClick={()=>{setDraft({...active.data,categoryOrder:(active.data.categoryOrder||categories).join(', ')});setModal('listSettings')}}>List settings & category order</DropdownMenuItem><DropdownMenuItem onClick={()=>{s.mutate('template',{name:active.data.name,items:outstanding.map(i=>i.data)});toast.success('Reusable template saved')}}>Save as template</DropdownMenuItem><DropdownMenuItem onClick={()=>setModal('templates')}>Add from template</DropdownMenuItem><DropdownMenuItem onClick={()=>setModal('history')}>Repeat a previous shop</DropdownMenuItem><DropdownMenuItem onClick={()=>{setDecisions({});setCompareItem(undefined);setModal('translate')}}>Shop in another country</DropdownMenuItem><DropdownMenuItem onClick={()=>setConfirm({title:'Archive this list?',description:'Items remain in your household export. Active shopping lists will no longer show this list.',run:()=>s.mutate('list',{...active.data,archived:true},active)})}>Archive list</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div><div className="chip-row shopper-bar" role="group" aria-label="Filter by shopper"><button type="button" className={'chip '+(shopper==='all'?'active':'')} onClick={()=>setShopper('all')}>Everyone <span>{tallies.all}</span></button><button type="button" className={'chip '+(shopper==='mine'?'active':'')} onClick={()=>setShopper('mine')}>Assigned to me <span>{tallies.mine}</span></button><button type="button" className={'chip '+(shopper==='unassigned'?'active':'')} onClick={()=>setShopper('unassigned')}>Unclaimed <span>{tallies.unassigned}</span></button>{s.members.filter(m=>m.user!==s.user?.id).map(m=><button type="button" key={m.user} className={'chip '+(shopper===m.user?'active':'')} onClick={()=>setShopper(m.user)}>{m.name.split(' ')[0]} <span>{tallies.members.find(x=>x.user===m.user)?.count||0}</span></button>)}</div></div>{outstanding.length===0?<div className="empty"><CheckCheck size={38}/><h3>{done.length?'Everything is in the basket.':'A fresh list, ready for anything.'}</h3><p>{done.length?'Save this trip to make the next shop easier.':'Add your first item above, or scan a family favourite.'}</p></div>:!visibleOutstanding.length?<div className="empty"><Users size={38}/><h3>Nothing in this shopper view.</h3><p>These items are still on the shared list. Switch to Everyone, or claim an unclaimed item for yourself.</p><button className="btn" onClick={()=>setShopper('all')}>Show everyone’s items</button></div>:[...new Set([...listCats,...visibleOutstanding.map(i=>i.data.category||'Other')])].map((category:any)=>{const group=visibleOutstanding.filter(i=>(i.data.category||'Other')===category);return group.length?<div className="category" key={category}><div className="category-label">{category}<span>{group.length}</span></div>{group.map(renderItem)}</div>:null})}{done.length>0&&<div className="completed-section"><div className="row between"><h3><CheckCheck size={18}/> In the basket <span>{done.length}</span></h3><button className="link" onClick={completeTrip}>Finish trip</button></div>{done.map(renderItem)}</div>}<div className="list-footer"><span><Users size={15}/> Tap a shopper initial to claim an item. Everyone sees the same list; unfinished items stay shared.</span></div></>}{!active&&<div className="empty"><ListChecks size={40}/><h3>One list. Less back and forth.</h3><button className="btn primary" onClick={()=>{setDraft({name:'Weekly groceries',currency});setModal('list')}}>Create a list</button></div>}</section><aside className="context-column"><section className="travel-card"><div className="row between"><span className="eyebrow">SAME FAVOURITES. NEW PLACE.</span><Globe2 size={25} strokeWidth={1.5}/></div><h2>Feels like home.<br/>Shops like local.</h2><p>Find your usual products—or a thoughtful alternative—in another country.</p><Choice label="Destination country" value={destination} onChange={setDestination} options={countries}/><button className="btn primary" onClick={()=>{setDecisions({});setCompareItem(undefined);setModal('translate')}}>Take this list abroad <ArrowRight size={17}/></button><small>Catalogue matches, with every swap in your hands.</small></section><section className="favourite-teaser"><div className="row between"><h3>Your usuals</h3><button className="link" onClick={()=>nav('favourites')}>View all</button></div>{favourites.slice(0,3).map(f=><div className="usual" key={f.id}><Photo product={{...f.data.product,image:f.data.image||f.data.product?.image}}/><div><strong>{f.data.product?.brand?.split(',')[0]||f.data.name}</strong><small className="muted">{f.data.product?.pack||'Household favourite'}</small></div><button className="iconbtn" aria-label={'Add favourite '+f.data.name} onClick={()=>add(f.data,undefined,true)}><Plus size={20}/></button></div>)}{!favourites.length&&<p className="muted">Save the exact products everyone asks for. They'll be one tap away.</p>}</section><section className="cost-card"><span className="eyebrow">BASKET NOTES</span><div className="row between"><span>Estimated priced items</span><strong>{fmt(money.estimated)}</strong></div><p className="muted">{money.missing} outstanding {money.missing===1?'item has':'items have'} no price. This is a partial estimate.</p>{done.length>0&&<p className="fine">{money.actualMissing} purchased items have no comparable actual price.</p>}{money.actual>0&&<div className="row between"><span>Recorded purchases</span><strong>{fmt(money.actual)}</strong></div>}</section></aside></div></>}
{view==='meals'&&<Meals key={hid} s={s} lists={lists} active={active} onAdd={add}/>}
{view==='discover'&&<><div className="page-heading"><div><span className="eyebrow">FAMILIAR FINDS, WHEREVER YOU ARE</span><h1>Find your next usual.</h1><p className="muted">Discover exact products, compare alternatives and find places to look.</p></div><button className="btn primary" onClick={()=>setModal('scanner')}><ScanBarcode size={18}/> Scan a product</button></div><Tabs defaultValue="products"><TabsList className="view-tabs"><TabsTrigger value="products">Product catalogue</TabsTrigger><TabsTrigger value="stores">Stores & offers</TabsTrigger></TabsList><TabsContent value="products"><div className="chip-row retailer-shortcuts" role="group" aria-label="Swiss retailer catalogues"><button type="button" className={'chip '+(searchRetailer===''?'active':'')} onClick={()=>browseRetailer('')}>All catalogues</button><button type="button" className={'chip '+(searchRetailer==='coop-ch'?'active':'')} onClick={()=>browseRetailer('coop-ch')}>Coop Switzerland</button><button type="button" className={'chip '+(searchRetailer==='migros-ch'?'active':'')} onClick={()=>browseRetailer('migros-ch')}>Migros</button></div><form className="catalogue-search" onSubmit={e=>{e.preventDefault();void search()}}><div className="search-input"><Search size={20}/><input aria-label="Search product names, brands or barcodes" placeholder="Product, brand or barcode" value={query} onChange={e=>setSearch(e.target.value)}/></div><Choice label="Catalogue country" value={searchCountry} onChange={v=>{searchSerial.current++;setBusy(false);setSearchCountry(v);if(searchRetailer&&retailers[searchRetailer].country!==v)setSearchRetailer('');setResults([]);setSearched(false);setSearchError('');setCoverage(undefined);setScope(undefined)}} options={{'':'All countries',...countries}}/><button className="btn primary" disabled={busy}>{busy?<Loader2 className="spin" size={18}/>:'Search'}</button></form><p className="fine">Search runs when you press Search. Coop and Migros shortcuts browse the saved Swiss index. Results are cached to respect Open Food Facts’ public service.</p><div className="discover-filters"><Choice label="Declared dietary label" value={dietFilter} onChange={v=>{searchSerial.current++;setBusy(false);setDietFilter(v);setResults([]);setSearched(false);setSearchError('')}} options={{'':'Any declared dietary label',vegan:'Vegan label',vegetarian:'Vegetarian label','gluten-free':'Gluten-free label',organic:'Organic label'}}/></div>{(scope||coverage)&&<div className="coverage-card">{scope?<><strong>{scope.communityRecords.toLocaleString()} {scope.tag==='coop'?'Coop':'Migros'} community records</strong><span> · {scope.communityPhotos.toLocaleString()} photo links · {scope.retailerLinks} retailer page titles · {scope.retailerPageDetails} with scraped facts</span><p>{scope.notice}</p></>:<p className="fine catalogue-coverage">{coverage.coverageByRetailer?`${coverage.records?.toLocaleString?.()||coverage.records} Swiss records · partial coverage`:`${coverage.imported||0} imported links · ${coverage.pageDetails||0} with page details · partial coverage`}</p>}</div>}{searchError&&<p className="notice" role="alert">{searchError}</p>}<div className="row wrap discover-manual"><button className="link" onClick={()=>openItem({name:query})}>Add a generic item</button><button className="link" onClick={()=>{setDraft({name:query});setPrivateEditing(undefined);setModal('private')}}>Create a private product</button></div><div className="row between section-title"><h2>{searched?(searchRetailer?retailers[searchRetailer].name+' results':'Search results'):'A few familiar faces'}</h2><span className="pill">{searchRetailer?retailers[searchRetailer].name:s.demo?'Demo catalogue':'Source-labelled catalogue'}</span></div>{searched&&!results.length&&!busy?<div className="empty card"><Search size={36}/><h3>{searchError?'No cached matches available.':'No products found.'}</h3><p>Try a brand or barcode. Household essentials can always be added manually.</p><button className="btn" onClick={()=>openItem({name:query})}>Add a generic item</button><button className="btn" onClick={()=>{setDraft({name:query});setPrivateEditing(undefined);setModal('private')}}>Create private product</button></div>:<div className="product-grid">{(searched?results:demoProducts).map(p=><article className="product-card" key={p.id}><button className="product-open" onClick={()=>void openProduct(p)}><Photo product={p} large/><span className="eyebrow">{p.brand?.split(',')[0]}</span><h3>{p.name}</h3><p className="muted">{p.pack||'Pack size not recorded'}{classifyRetailerProduct(p)==='household'?' · Household goods':''}</p></button><div className="row between"><button className="iconbtn" aria-label={'Review '+p.name+' to save as favourite'} onClick={()=>void openProduct(p)}><Heart size={20}/></button><button className="btn" onClick={()=>void openProduct(p)}><Plus size={16}/> Review & add</button></div></article>)}</div>}<p className="source-note">Community product data: <a href="https://world.openfoodfacts.org" target="_blank" rel="noreferrer">Open Food Facts</a> · ODbL 1.0. Its product images: CC BY-SA 3.0. Retailer imports have separate page sources; they are not licensed under ODbL. <button className="link" onClick={()=>setModal('about')}>Sources & limitations</button></p></TabsContent><TabsContent value="stores"><StoreHub s={s} active={active} onProduct={p=>void openProduct(p)} onAdd={d=>d.product?.source==='Open Food Facts'?void openProduct(d.product,d.store):add(d)}/><div className="section-title"><h2>Spotted by your household</h2><p className="muted">Dated member reports, not verified stock or current prices.</p></div>{s.records.filter(r=>r.kind==='observation').length===0?<div className="empty card">Record where you bought an item from its list menu.</div>:s.records.filter(r=>r.kind==='observation').map(r=><article className="card observation" key={r.id}><MapPin size={20}/><div><h3>{r.data.name}</h3><p>{r.data.store} · {r.data.date}</p><small className="muted">Reported by {memberName(r.createdBy)}{r.data.price!==''&&r.data.price!=null?' · '+r.data.price+' '+r.data.currency:''}. Stock may have changed.</small></div></article>)}</TabsContent></Tabs></>}
{view==='favourites'&&<><div className="page-heading"><div><span className="eyebrow">THE ONES WORTH REMEMBERING</span><h1>Your family favourites.</h1><p className="muted">The right brand. The right variant. That note you don't want to forget.</p></div><button className="btn primary" onClick={()=>setModal('scanner')}><ScanBarcode size={18}/> Scan a favourite</button></div>{!favourites.length?<div className="empty card"><Heart size={38}/><h3>Start with something you love.</h3><p>Scan or search for a product and save it here.</p><button className="btn" onClick={()=>nav('discover')}>Discover products</button></div>:<div className="product-grid">{favourites.map(f=><article className="product-card favourite-card" key={f.id}><button className="product-open" onClick={()=>void openProduct(f.data.product)}><Photo product={{...f.data.product,image:f.data.image||f.data.product?.image}} large/><span className="eyebrow">{f.data.product?.brand?.split(',')[0]||'YOUR HOUSEHOLD'}</span><h3>{f.data.name}</h3></button><p className="muted">Usually {f.data.quantity||1} {f.data.unit||'pack'} · {f.data.pack||'Pack not recorded'}</p>{f.data.notes&&<p className="favourite-note">“{f.data.notes}”</p>}<div className="row between"><button className="iconbtn" aria-label={'Edit favourite '+f.data.name} onClick={()=>{setDraft(f.data);setEditing(f);setModal('favourite')}}><Settings2 size={18}/></button><button className="btn primary" onClick={()=>add(f.data,undefined,true)}><Plus size={17}/> Same again</button></div></article>)}</div>}<section className="section-title"><h2>Remembered abroad</h2><p className="muted">Substitutes your household approved for a specific country.</p></section><div className="grid2">{s.records.filter(r=>r.kind==='substitution').map(r=><article className="card row" key={r.id}><Photo product={r.data.product}/><div><strong>{r.data.product?.name}</strong><p className="muted">{countries[r.data.country]} · {r.data.reason}</p></div><button className="iconbtn" aria-label="Add accepted alternative" onClick={()=>add({product:r.data.product,name:r.data.product.name,pack:r.data.product.pack})}><Plus/></button></article>)}</div></>}
{view==='household'&&<><div className="page-heading"><div><span className="eyebrow">BETTER, TOGETHER</span><h1>{household.name}</h1><p className="muted">Shared favourites. Individual tastes. Everyone has a place.</p></div><button className="btn primary" disabled={!['owner','admin'].includes(household.role)} onClick={()=>{setDraft({email:''});setModal('invitePerson')}}><Plus size={18}/> Invite someone</button></div><div className="household-grid"><section className="card"><h2>The people on your list</h2>{s.members.map(m=><div className="member-row" key={m.user}><span className="avatar">{m.name.slice(0,1)}</span><div><strong>{m.name}{m.user===s.user?.id?' (you)':''}</strong><small className="muted">{m.role}</small></div>{m.user!==s.user?.id&&household.role==='owner'&&<DropdownMenu><DropdownMenuTrigger asChild><button className="iconbtn" aria-label={'Manage '+m.name}><MoreHorizontal size={20}/></button></DropdownMenuTrigger><DropdownMenuContent><DropdownMenuItem onClick={()=>act('role',{user:m.user,role:m.role==='admin'?'member':'admin'})}>{m.role==='admin'?'Make member':'Make administrator'}</DropdownMenuItem><DropdownMenuItem onClick={()=>setConfirm({title:'Transfer ownership to '+m.name+'?',description:'You will become an administrator. The new owner will control membership and deletion.',run:()=>act('transfer',{user:m.user})})}>Transfer ownership</DropdownMenuItem><DropdownMenuItem onClick={()=>setConfirm({title:'Remove '+m.name+'?',description:'They will lose access to this household.',run:()=>act('remove',{user:m.user})})}>Remove member</DropdownMenuItem></DropdownMenuContent></DropdownMenu>}{household.role==='admin'&&m.role==='member'&&m.user!==s.user?.id&&<button className="iconbtn" aria-label={'Remove '+m.name} onClick={()=>setConfirm({title:'Remove member?',description:'They will lose access to this household.',run:()=>act('remove',{user:m.user})})}><X size={18}/></button>}</div>)}<p className="fine">An invite is single-use, expires in 7 days and can be revoked. The person joining signs in with their own ChatGPT account.</p>{s.invites.filter(i=>!i.used_by&&!i.revoked).map(i=><div className="row between invitation" key={i.id}><span>{i.recipient_email||'Invitation'} · {i.expires<Date.now()?'Expired':'Pending'} · {new Date(i.expires).toLocaleDateString()}</span><button className="link" onClick={()=>act('revoke',{id:i.id},'Invitation revoked')}>Revoke</button></div>)}</section><section className="card stack"><h2>Make yourself at home</h2>{!s.demo&&<label>Switch household<Choice label="Switch household" value={hid} onChange={s.switchHousehold} options={Object.fromEntries(s.households.map(h=>[h.id,h.name]))}/></label>}<div className="row between"><span>Dark mode</span><Switch checked={dark} onCheckedChange={changeTheme} aria-label="Dark mode"/></div><button className="btn" onClick={()=>{setDraft({name:household.name,...household.settings,constraintsText:(household.settings.constraints||[]).map((c:any)=>c.kind+':'+c.value).join('\n')});setModal('settings')}}><Settings2 size={18}/> Household preferences</button><button className="btn" onClick={()=>{setDraft({name:s.user?.name,...s.user?.preferences,constraintsText:(s.user?.preferences?.constraints||[]).map((c:any)=>c.kind+':'+c.value).join('\n')});setModal('profile')}}><UserRound size={18}/> My preferences</button><button className="btn" onClick={()=>{setDraft({name:'',country:'IT',currency:'EUR',language:'en',listName:'Weekly groceries'});setModal('create')}}><Plus size={18}/> Create another household</button><button className="btn" onClick={()=>{setDraft({token:''});setModal('join')}}>Join with an invitation</button></section><section className="card"><h2>Your data, your choice</h2><p className="muted">Export your account and shared household records. If you leave, the household keeps its shopping history.</p><div className="row wrap"><a className="btn" href={s.demo?'#':'/api/export'} onClick={e=>{if(s.demo){e.preventDefault();toast('Exports are available in your own household.')}}}><Download size={17}/> Export my data</a><button className="btn" onClick={()=>setConfirm({title:'Leave this household?',description:household.role==='owner'?'Transfer ownership to another member first. A household must have an owner.':'Your access will end. Shared shopping records will remain.',run:()=>act('leave',{},'You left the household')})}>Leave household</button><button className="btn danger" onClick={()=>setConfirm({title:'Delete your account data?',description:'Transfer ownership first. Shared records stay with the family and your contributions are anonymised. Your profile and memberships are deleted.',run:async()=>{const r=await act('deleteAccount');if(r){for(const k of Object.keys(localStorage))if(k.startsWith('same-again:'))localStorage.removeItem(k);window.location.href='/signout-with-chatgpt?return_to=%2F'}}})}>Delete account data</button>{household.role==='owner'&&<button className="btn danger" onClick={()=>setConfirm({title:'Permanently delete this household?',description:'All its shared lists, favourites and history will be deleted for everyone. Export first if you need a copy.',run:async()=>{const r=await act('deleteHousehold');if(r)window.location.reload()}})}>Delete household</button>}</div></section></div><div className="row wrap section-title"><a className="btn" href="/device-check" target="_blank">Device check</a><button className="btn" onClick={()=>setModal('archives')}>Archived lists</button><button className="btn" onClick={s.enterDemo}>Open separate demo</button><a className="btn" href="/signout-with-chatgpt?return_to=%2F" target="_top" onClick={e=>{if(s.pending){e.preventDefault();toast.error('Sync pending changes before signing out.');return}for(const k of Object.keys(localStorage))if(k.startsWith('same-again:')&&k!=='same-again:theme')localStorage.removeItem(k)}}><LogOut size={17}/> Sign out & clear device cache</a></div></>}
</>}</main><nav className="bottom-nav" aria-label="Mobile navigation">{navigation.map(n=><button key={n.id} aria-current={view===n.id?'page':undefined} className={view===n.id?'active':''} onClick={()=>nav(n.id)}><n.icon size={22}/><span>{n.label}</span></button>)}<button className="scan-fab" onClick={()=>setModal('scanner')} aria-label="Scan barcode"><ScanBarcode size={26}/></button></nav></div>
<Modal open={modal==='item'} onClose={()=>setModal('')} title={editing?'Make it just right':'Add something to the list'} wide><form className="formgrid" onSubmit={saveItem}><label className="full">Item name<input required maxLength={160} value={draft.name||''} onChange={e=>setDraft({...draft,name:e.target.value})}/></label><label>Quantity<input required type="number" min="0.01" max="10000" step="any" value={draft.quantity||1} onChange={e=>setDraft({...draft,quantity:Number(e.target.value)})}/></label><label>Unit<Choice label="Unit" value={draft.unit||'pack'} onChange={v=>setDraft({...draft,unit:v})} options={units}/></label><label>Pack size<input placeholder="e.g. 500 g" value={draft.pack||''} onChange={e=>setDraft({...draft,pack:e.target.value})}/></label><label>Category<input list="categories" value={draft.category||''} onChange={e=>setDraft({...draft,category:e.target.value})}/><datalist id="categories">{categories.map((c:string)=><option key={c}>{c}</option>)}</datalist></label><details className="item-photo-editor full"><summary><Plus size={16}/> {draft.image?'Change household photo':'Add a household photo'}</summary><PhotoUpload household={hid} demo={s.demo} value={draft.image} onChange={image=>setDraft((d:any)=>({...d,image}))} onBusy={setPhotoBusy}/><p className="fine">Your photo stays with this item and repeat purchases. Catalogue photos and facts stay separate.</p></details><label className="full">A note for your shopper<textarea rows={2} maxLength={800} placeholder="The unsweetened one…" value={draft.notes||''} onChange={e=>setDraft({...draft,notes:e.target.value})}/></label><label>Shopping list<Choice label="Shopping list" value={draft.list||active?.id||''} onChange={v=>setDraft({...draft,list:v})} options={Object.fromEntries(lists.map(l=>[l.id,l.data.name]))}/></label><label>Assigned shopper<Choice label="Assigned shopper" value={draft.assigned||''} onChange={v=>setDraft({...draft,assigned:v})} options={{'':'Anyone',...Object.fromEntries(s.members.map(m=>[m.user,m.name]))}}/></label><label>Substitution preference<Choice label="Substitution preference" value={draft.substitution||'similar'} onChange={v=>setDraft({...draft,substitution:v})} options={substitutions}/></label><label>Intended for<Choice label="Intended for" value={draft.intendedFor||''} onChange={v=>setDraft({...draft,intendedFor:v})} options={{'':'Household + my preferences',...Object.fromEntries(s.members.map(m=>[m.user,m.name]))}}/></label><label>Preferred store<input value={draft.store||''} onChange={e=>setDraft({...draft,store:e.target.value})}/></label><label>Priority<Choice label="Priority" value={draft.priority||'normal'} onChange={v=>setDraft({...draft,priority:v})} options={{normal:'Normal',high:'Important'}}/></label><label>Estimated price per unit ({currency})<input type="number" step="0.01" min="0" value={draft.price??''} onChange={e=>setDraft({...draft,price:e.target.value===''?null:Number(e.target.value)})}/></label><label>Actual price per unit ({currency})<input type="number" step="0.01" min="0" value={draft.actualPrice??''} onChange={e=>setDraft({...draft,actualPrice:e.target.value===''?null:Number(e.target.value)})}/></label>{draft.receipt&&<p className="notice full">From a reviewed receipt · {draft.receipt.store||'Store not recorded'}{draft.receipt.date?' · '+draft.receipt.date:''}. {draft.receipt.lineTotal!=null?'Past line total: '+draft.receipt.lineTotal.toFixed(2)+' '+draft.receipt.currency+'. ':''}Receipt descriptions are not verified product identities or current prices.</p>}{editing&&<p className="fine full">Added by {memberName(editing.createdBy)} · {new Date(editing.created).toLocaleString()}. Last updated {new Date(editing.updated).toLocaleString()}.</p>}<div className="full row between">{editing?<button type="button" className="iconbtn danger" aria-label="Delete item" onClick={()=>remove(editing)}><Trash2 size={20}/></button>:<span/>}<button className="btn primary" type="submit" disabled={photoBusy}>{editing?'Save changes':'Add to list'} <Check size={18}/></button></div></form></Modal>
<Modal open={modal==='receipt'} onClose={()=>setModal('')} title="Buy it again, from a receipt." description="Scan, review, then add selected items to your shared list." wide>{modal==='receipt'&&household&&<ReceiptScanner key={hid} lists={lists} active={active?.id||''} country={household.settings.country||''} currency={currency} onAdd={addReceipt}/>}</Modal>
<Modal open={modal==='scanner'} onClose={()=>setModal('')} title="Scan your usual." description="One scan. Exactly the right product."><Scanner onCode={code=>search(code)} onPrivate={code=>{setDraft({barcode:code,name:'',brand:'',pack:''});setPrivateEditing(undefined);setModal('private')}}/></Modal>
<Modal open={modal==='product'} onClose={()=>{productSerial.current++;setProductLoading(false);setModal('')}} title={product?.name||'Product details'} description={product?.source||'Product information'} wide>{product&&<div className="stack"><div className="product-detail-heading"><Photo product={product} large/><div><span className="eyebrow">{product.brand?.split(',').map(v=>v.trim()).filter(Boolean).slice(0,2).join(' · ')}</span><p>{product.pack||'Pack size not recorded'}</p><span className="pill">{product.barcode||(product.source==='Open Food Facts'||product.retailer?'Source record · standard barcode unknown':'Private product')}</span>{productStore&&<p className="selected-product-store"><MapPin size={15}/>{productStore}</p>}</div></div>{productLoading&&<p className="notice row" role="status"><Loader2 className="spin" size={18}/> Checking full product details…</p>}{productNotice&&<p className="notice" role="status">{productNotice}</p>}{product.source==='Open Food Facts'&&product.barcode&&!s.demo&&<button className="link" disabled={productLoading} onClick={()=>void openProduct(product,productStore)}>Refresh product details</button>}{product.barcode&&barcode(product.barcode).local&&<p className="notice">This may be a retailer-specific or variable-weight code. Confirm the product and store context.</p>}<div className="row wrap product-actions"><button className="btn primary" disabled={productLoading} onClick={()=>{add({product,name:product.name,pack:product.pack,store:productStore||active?.data.store});setModal('')}}><Plus size={18}/> Add to {active?.data.name||'list'}</button><button className="btn" disabled={productLoading} onClick={()=>saveFavourite({product,name:product.name,pack:product.pack,quantity:1,unit:'pack'})}><Heart size={18}/> Save favourite</button><button className="btn" disabled={productLoading} onClick={()=>openItem({product,name:product.name,pack:product.pack,store:productStore||active?.data.store})}>Add with a note</button></div>{s.records.some(r=>r.kind==='product'&&r.id===product.id)&&<button className="btn" onClick={()=>{const r=s.records.find(r=>r.kind==='product'&&r.id===product.id)!;setPrivateEditing(r);setDraft({...r.data,allergens:r.data.allergens?.join(', ')||'',traces:r.data.traces?.join(', ')||''});setModal('private')}}>Edit private product</button>}{product.evidence&&<p className="notice">{product.evidence==='indexed-link'?'This entry comes from an indexed retailer page title. The product details and current assortment have not been verified. Open the retailer page to check.':'Details were extracted from a public retailer product page on the date shown below. Current branch stock is unknown.'}</p>}{product.nutritionNote&&<p className="fine">{product.nutritionNote}</p>}<section><h3>Ingredients</h3><p className="muted">{product.ingredients||'Ingredients are not recorded. Check the package.'}</p><div className="grid2"><div><strong>Declared allergens</strong><p>{product.allergens?product.allergens.length?product.allergens.map(a=>a.replace(/^en:/,'')).join(', '):'None in the catalogue declaration':'Not recorded'}</p></div><div><strong>Declared traces</strong><p>{product.traces?product.traces.length?product.traces.map(a=>a.replace(/^en:/,'')).join(', '):'None in the catalogue declaration':'Not recorded'}</p></div></div><p className="notice">Missing information is not confirmation of suitability. Check the current package label, especially for allergies.</p></section><ProductNutrition product={product}/><IngredientReview product={product} constraints={[...(household.settings.constraints||[]),...(s.user.preferences?.constraints||[])]}/><p className="fine">Applying household requirements and {s.user.name}’s personal preferences.</p><section><h3>Retailer evidence</h3><p className="muted">{product.stores?.length?product.stores.join(', '):'Retailer tags are not recorded'}. Community catalogue tags do not establish current branch stock.</p></section><section><h3>Country records</h3><p className="muted">{product.countries.length?product.countries.map(c=>c.replace(/^en:/,'').replaceAll('-',' ')).join(', '):'No countries recorded'}. These records do not establish current availability.</p></section><section><h3>What does your family think?</h3><div className="row wrap">{['Like','Dislike','Would buy again','Not a suitable substitute'].map(v=><button key={v} className="btn" onClick={()=>{setDraft({product:product.id,feedback:v,reason:''});setModal('feedback')}}>{v}</button>)}</div>{s.records.filter(r=>r.kind==='feedback'&&r.data.product===product.id).map(r=><p className="fine" key={r.id}>{memberName(r.data.user)}: {r.data.feedback}{r.data.reason?' — '+r.data.reason:''}</p>)}</section><p className="source-note">{product.sourceUrl?<a href={product.sourceUrl} target="_blank" rel="noreferrer">View nutrition & product source</a>:product.source} · Retrieved {new Date(product.retrieved).toLocaleDateString()}.{product.sourceUpdated&&<> Source updated {new Date(product.sourceUpdated).toLocaleDateString()}.</>}{product.indexedAt&&<> Search index dated {product.indexedAt.slice(0,10)}; check the package for current details.</>} <button className="link" onClick={()=>setModal('about')}>Licensing</button></p><a className="btn" href={'https://www.google.com/search?q='+encodeURIComponent(product.name+' '+(countries[destination]||''))} target="_blank" rel="noreferrer">Search the web for this product <ExternalLink size={16}/></a></div>}</Modal>
<Modal open={modal==='private'} onClose={()=>setModal('')} title="Not in the catalogue? Make it yours." description="Saved privately to this household. Nothing is published to Open Food Facts."><form className="stack" onSubmit={async e=>{e.preventDefault();if(draft.barcode&&!barcode(draft.barcode).valid)return toast.error('Check the barcode digits.');const p:any={id:uid(),name:draft.name,brand:draft.brand||'',pack:draft.pack||'',barcode:draft.barcode?barcode(draft.barcode).code:'',image:draft.image,ingredients:draft.ingredients||undefined,allergens:draft.allergens?draft.allergens.split(',').map((v:string)=>v.trim()).filter(Boolean):undefined,traces:draft.traces?draft.traces.split(',').map((v:string)=>v.trim()).filter(Boolean):undefined,nutrition:draft.nutrition||{},basis:draft.basis||'100g',categories:[],countries:[],source:'User-entered · private household data',retrieved:Date.now()};const saved=s.mutate('product',p,privateEditing);if(saved){p.id=saved.id;if(!privateEditing)add({product:p,name:p.name,pack:p.pack,notes:draft.notes||''});else{setProduct(p);toast.success('Private product updated. Existing recipes retain their saved version.')}setModal(privateEditing?'product':'')}}}><label>Name<input required maxLength={160} value={draft.name||''} onChange={e=>setDraft({...draft,name:e.target.value})}/></label><div className="grid2"><label>Brand<input value={draft.brand||''} onChange={e=>setDraft({...draft,brand:e.target.value})}/></label><label>Pack size<input value={draft.pack||''} onChange={e=>setDraft({...draft,pack:e.target.value})}/></label></div><label>Barcode (optional)<input inputMode="numeric" value={draft.barcode||''} onChange={e=>setDraft({...draft,barcode:e.target.value})}/></label><PhotoUpload household={hid} demo={s.demo} onBusy={setBusy} value={draft.image} onChange={image=>setDraft((d:any)=>({...d,image}))}/><label>Ingredients as printed on the label<textarea maxLength={6000} value={draft.ingredients||''} onChange={e=>setDraft({...draft,ingredients:e.target.value})}/></label><div className="grid2"><label>Declared allergens · comma separated<input maxLength={500} value={draft.allergens||''} onChange={e=>setDraft({...draft,allergens:e.target.value})}/></label><label>Declared traces · comma separated<input maxLength={500} value={draft.traces||''} onChange={e=>setDraft({...draft,traces:e.target.value})}/></label></div><Choice label="Label nutrition basis" value={draft.basis||'100g'} onChange={basis=>setDraft({...draft,basis})} options={{'100g':'Label values per 100 g','100ml':'Label values per 100 ml'}}/><NutritionFields value={draft.nutrition||{}} onChange={nutrition=>setDraft({...draft,nutrition})}/><p className="fine">Copy explicit label values. Leave missing information blank; it does not confirm suitability.</p><label>Note<input value={draft.notes||''} onChange={e=>setDraft({...draft,notes:e.target.value})}/></label><button disabled={busy} className="btn primary">{privateEditing?'Save private product':'Save private product & add to list'}</button></form></Modal>
<Modal open={modal==='translate'} onClose={()=>{matchSerial.current++;setBusy(false);setModal('');setCompareItem(undefined)}} title="Same list. Somewhere new." description="Approve each choice, then create a destination copy. Your original stays as it is." wide><div className="stack"><div className="grid2"><label>Where are you shopping?<Choice label="Destination country" value={destination} onChange={v=>{matchSerial.current++;setBusy(false);setDestination(v);setDecisions({});setCompareItem(undefined);setMatches([])}} options={countries}/></label><label>What matters most?<Choice label="Matching priority" value={priority} onChange={v=>{matchSerial.current++;setBusy(false);setPriority(v);setCompareItem(undefined);setMatches([])}} options={{ingredients:'Similar ingredients',nutrition:'Similar nutritional profile',use:'Same intended use',pack:'Similar pack size',brand:'Same brand'}}/></label></div>{compareItem?<><button className="link row" onClick={()=>setCompareItem(undefined)}><ArrowLeft size={17}/> Back to your basket</button><div className="original-card row"><Photo product={compareItem.data.product}/><div><span className="eyebrow">YOUR USUAL</span><h3>{compareItem.data.name}</h3><p className="muted">{compareItem.data.quantity} × {compareItem.data.pack||compareItem.data.unit}</p></div></div><p className="fine">Applying household constraints and {compareItem.data.intendedFor?memberName(compareItem.data.intendedFor):s.user?.name}'s personal requirements. Intended use is inferred from catalogue category; taste is not inferred.</p>{busy?<div className="empty"><Loader2 className="spin"/><p>Comparing the available evidence…</p></div>:matches.length?matches.map(m=><article className="candidate card" key={m.product.id}><span className="pill">{m.band}</span><div className="row"><Photo product={m.product} large/><div><h3>{m.product.name}</h3><p>{m.product.pack||'Pack size unknown'}</p><small className="muted">Recorded for {countries[destination]} · Open Food Facts</small></div></div><div className="grid2"><div><strong>Why it is a candidate</strong><ul>{m.reasons.map((r:string)=><li key={r}>{r}</li>)}</ul></div><div><strong>What to check</strong><ul>{[...m.differences,...m.unknown,m.taste].map((r:string)=><li key={r}>{r}</li>)}</ul></div></div><p className="fine">{(()=>{const c=compareItem.data.unit==='pack'?conversion(compareItem.data.pack,m.product.pack,compareItem.data.quantity):null;return c?`Your original is ${c.original} ${c.unit}. This works out to ${Number(c.exact.toFixed(2))} of these packs. Choose how many you want.`:'Pack quantities cannot be reliably converted. Choose a quantity after checking the package.'})()}</p><form className="row" onSubmit={e=>{e.preventDefault();const n=Number(new FormData(e.currentTarget).get('quantity'));choose(compareItem,m.product,m.reasons.join('; '),n)}}><label>New quantity<input name="quantity" type="number" min="0.01" max="10000" step="any" required defaultValue={compareItem.data.quantity}/></label><button className="btn primary">Approve this alternative</button></form><a className="fine" href={m.product.sourceUrl} target="_blank" rel="noreferrer">Source · retrieved {new Date(m.product.retrieved).toLocaleDateString()}</a></article>):<div className="empty card"><Package size={34}/><h3>No confident match.</h3><p>There isn't enough compatible catalogue evidence to suggest a credible alternative.</p></div>}<p className="fine">{matchReason}</p><div className="row wrap"><button className="btn" onClick={()=>choose(compareItem,compareItem.data.product,'Kept the original product')}>Keep original</button>{compareItem.data.substitution!=='exact'&&<button className="btn" onClick={()=>choose(compareItem,null,'User chose a generic item')}>Keep generic</button>}<button className="btn" onClick={()=>{setModal('');nav('discover');setSearchCountry(destination);setQuery(compareItem.data.product?.brand||compareItem.data.name)}}>Search catalogue again</button></div></>:<><div className="translation-items">{outstanding.map(item=>{const p=item.data.product,decision=decisions[item.id],recorded=p?.countries?.includes(countryTag(destination));return <article key={item.id} className="translation-row"><div className="row"><Photo product={p}/><div><h3>{item.data.name}</h3><small className="muted">{item.data.quantity} × {item.data.pack||item.data.unit}</small></div></div><div><span className="country-evidence">{p?recorded?'Recorded for '+countries[destination]:'No country record for '+countries[destination]:'Generic item'}</span>{decision?<div className="approved"><Check size={16}/><span>{decision.product?.name||'Keep generic'} · {decision.quantity}</span><button className="iconbtn" aria-label="Clear decision" onClick={()=>setDecisions(d=>{const next={...d};delete next[item.id];return next})}><X size={16}/></button></div>:<div className="row wrap">{p&&<button className="btn" onClick={()=>findMatches(item)}>Compare <ArrowRight size={15}/></button>}<button className="link" onClick={()=>choose(item,p||null,p?'Kept original product':'Kept generic item')}>{p?'Keep original':'Keep generic'}</button></div>}</div></article>})}</div>{!outstanding.length&&<p className="empty">Add some outstanding items to your list first.</p>}<div className="row between"><span className="muted">{Object.keys(decisions).length} of {outstanding.length} reviewed</span><button className="btn primary" disabled={!outstanding.length||Object.keys(decisions).length!==outstanding.length} onClick={translate}>Create destination list <ArrowRight size={18}/></button></div></>}<p className="notice">Catalogue country records are not stock information. Check current labels. No substitutions happen without your approval.</p></div></Modal>
<Modal open={['create','list','listSettings'].includes(modal)} onClose={()=>setModal('')} title={modal==='create'?'A household starts here.':modal==='listSettings'?'List settings':'A new list, less to remember.'}><form className="stack" onSubmit={async e=>{e.preventDefault();if(modal==='create'){const r=await act('createHousehold',draft,'Your household is ready');if(r){s.exitDemo();s.switchHousehold(r.household);setSelected('');setModal('')}}else if(modal==='listSettings'&&active){s.mutate('list',{...draft,categoryOrder:draft.categoryOrder.split(',').map((v:string)=>v.trim()).filter(Boolean)},active);setModal('')}else{const l=s.mutate('list',{...draft,country:household.settings.country,currency:draft.currency||currency});if(l)setSelected(l.id);setModal('')}}}><label>{modal==='create'?'Household name':'List name'}<input autoFocus required maxLength={80} placeholder={modal==='create'?'e.g. The Sunday household':'e.g. Weekend dinner'} value={draft.name||''} onChange={e=>setDraft({...draft,name:e.target.value})}/></label>{modal==='create'&&<><label>First list<input required value={draft.listName||''} onChange={e=>setDraft({...draft,listName:e.target.value})}/></label><label>Usual shopping country · optional<Choice label="Usual shopping country" value={draft.country||'IT'} onChange={v=>setDraft({...draft,country:v,currency:v==='US'?'USD':v==='GB'?'GBP':v==='CH'?'CHF':'EUR'})} options={countries}/></label><label>Language preference · optional<Choice label="Language preference" value={draft.language||'en'} onChange={v=>setDraft({...draft,language:v})} options={{en:'English',it:'Italiano',fr:'Français',de:'Deutsch',es:'Español'}}/></label><p className="fine">The interface is currently in English. Your preferred language is saved for future localisation.</p></>}{modal!=='create'&&<label>Preferred store · optional<input value={draft.store||''} onChange={e=>setDraft({...draft,store:e.target.value})}/></label>}<label>Currency<Choice label="Currency" value={draft.currency||'EUR'} onChange={v=>setDraft({...draft,currency:v})} options={{EUR:'EUR · Euro',USD:'USD · US dollar',CHF:'CHF · Swiss franc',GBP:'GBP · Pound sterling',DKK:'DKK · Danish krone',SEK:'SEK · Swedish krona',NOK:'NOK · Norwegian krone',PLN:'PLN · Polish złoty',CZK:'CZK · Czech koruna',HUF:'HUF · Hungarian forint',RON:'RON · Romanian leu',ISK:'ISK · Icelandic króna'}}/></label>{modal==='listSettings'&&<label>Category order, separated by commas<textarea value={draft.categoryOrder||''} onChange={e=>setDraft({...draft,categoryOrder:e.target.value})}/><span className="fine">Your own shopping order, not a verified store aisle map.</span></label>}<button className="btn primary" disabled={busy}>{modal==='listSettings'?'Save settings':'Create '+(modal==='create'?'household':'list')} <ArrowRight size={18}/></button></form></Modal>
<Modal open={modal==='join'} onClose={()=>setModal('')} title="There's room for you." description="Join with a secure invitation from a household administrator."><form className="stack" onSubmit={async e=>{e.preventDefault();let token=draft.token?.trim();try{if(token.startsWith('http'))token=new URL(token).searchParams.get('invite')}catch{}const r=await act('join',{token},'Welcome to the household');if(r){s.exitDemo();s.switchHousehold(r.household);setModal('');history.replaceState({},'',window.location.pathname)}}}><p className="fine">If another person cannot open Same Again, its owner needs to grant them Site access first. This household link cannot change that permission.</p><label>Invitation link or code<textarea required value={draft.token||''} onChange={e=>setDraft({...draft,token:e.target.value})}/></label>{s.realUser?<button className="btn primary" disabled={busy}>Join household <Users size={18}/></button>:<a className="btn primary" href={'/signin-with-chatgpt?return_to='+encodeURIComponent('/?invite='+encodeURIComponent(draft.token||''))} target="_top">Sign in to join</a>}</form></Modal>
<Modal open={modal==='invitePerson'} onClose={()=>setModal('')} title="Invite a family member" description="Their Site access and household membership are separate. Both are needed."><form className="stack" onSubmit={async e=>{e.preventDefault();const r=await act('invite',{email:draft.email},'Invitation created');if(r){setInviteUrl(window.location.origin+'/?invite='+r.token);setModal('invite')}}}><label>Their ChatGPT sign-in email<input type="email" required maxLength={254} value={draft.email||''} onChange={e=>setDraft({...draft,email:e.target.value})}/></label><ol><li>The Site owner adds this email as a viewer in the Site’s sharing controls in ChatGPT.</li><li>Create and send the household link below. Only this signed-in email can accept it.</li></ol><p className="fine">Creating this link does not send an email or grant Site access. Existing household data stays restricted to household members.</p><button disabled={busy} className="btn primary">Create private household invitation</button></form></Modal>
<Modal open={modal==='invite'} onClose={()=>setModal('')} title="Good things are shared." description="Give this single-use link to one person. They need Site access and must sign in with ChatGPT. It expires in 7 days."><div className="notice"><strong>First, give your family member Site access.</strong><p>The Site owner must add them using this Site’s sharing controls in ChatGPT. A household invitation does not grant Site access. Ask them to open the Site before sending this household link.</p><p>If they see “Access denied” before Same Again opens, contact the Site owner. An expired household link can be replaced here.</p></div><label>Private invitation<input readOnly value={inviteUrl}/></label><div className="row"><button className="btn primary" onClick={async()=>{try{await navigator.clipboard.writeText(inviteUrl);toast.success('Invitation copied')}catch{toast('Select and copy the invitation link above.')}}}><Copy size={17}/> Copy link</button>{typeof navigator!=='undefined'&&!!navigator.share&&<button className="btn" onClick={()=>navigator.share({title:'Join our Same Again household',url:inviteUrl}).catch(()=>{})}><Share2 size={17}/> Share</button>}</div></Modal>
<Modal open={modal==='archives'} onClose={()=>setModal('')} title="Archived lists"><div className="stack">{s.records.filter(r=>r.kind==='list'&&r.data.archived).map(r=><div className="row between" key={r.id}><strong>{r.data.name}</strong><button className="btn" onClick={()=>{s.mutate('list',{...r.data,archived:false},r);setSelected(r.id);setModal('');nav('lists')}}>Restore list</button></div>)}{!s.records.some(r=>r.kind==='list'&&r.data.archived)&&<p>No archived lists.</p>}</div></Modal>
<Modal open={modal==='history'||modal==='templates'} onClose={()=>setModal('')} title={modal==='history'?'The good ones, again.':'A head start on your next list.'} description="Choose items to add to your active shopping list. Notes, exact products and quantities are preserved." wide><div className="stack">{(modal==='history'?trips:templates).length===0?<div className="empty"><History size={36}/><p>{modal==='history'?'Finish a shopping trip to find it here.':'Save a list as a template from its options menu.'}</p></div>:(modal==='history'?trips:templates).map(t=><form className="card stack" key={t.id} onSubmit={e=>{e.preventDefault();const selectedItems=new FormData(e.currentTarget).getAll('repeat').map(Number);for(const i of selectedItems)add(t.data.items[i],undefined,true);if(selectedItems.length)setModal('');else toast('Select at least one item.')}}><div className="row between"><h3>{t.data.name}</h3><span className="fine">{new Date(t.data.date||t.created).toLocaleDateString()}</span></div>{t.data.items?.map((item:any,i:number)=><label className="row history-item" key={i}><Checkbox name="repeat" value={String(i)} defaultChecked className="history-check"/><Photo product={{...item.product,name:item.name,image:item.image||item.product?.image}}/><span>{item.name}<small className="muted">{item.quantity} {item.unit} · {item.notes||item.pack}</small></span></label>)}{modal==='templates'&&<button type="button" className="link danger" onClick={()=>remove(t)}>Delete template</button>}<button className="btn primary">Add selected to {active?.data.name||'active list'} <Repeat2 size={18}/></button></form>)}</div></Modal>
<Modal open={modal==='favourite'} onClose={()=>setModal('')} title="Remember it your way."><form className="stack" onSubmit={e=>{e.preventDefault();s.mutate('favourite',draft,editing);setModal('')}}><label>Usual quantity<input type="number" min="0.01" step="any" value={draft.quantity||1} onChange={e=>setDraft({...draft,quantity:Number(e.target.value)})}/></label><label>Preferred variant & notes<textarea value={draft.notes||''} onChange={e=>setDraft({...draft,notes:e.target.value})}/></label><label>Substitution preference<Choice label="Favourite substitution preference" value={draft.substitution||'similar'} options={substitutions} onChange={v=>setDraft({...draft,substitution:v})}/></label><div className="row between"><button className="btn" type="button" onClick={()=>{if(editing)remove(editing)}}>Remove favourite</button><button className="btn primary">Save favourite</button></div></form></Modal>
<Modal open={modal==='feedback'} onClose={()=>setModal('')} title={draft.feedback||'Your feedback'} description="This is your personal opinion. It won't become a restriction for everyone."><form className="stack" onSubmit={e=>{e.preventDefault();s.mutate('feedback',{...draft,user:s.user.id});setModal('product');toast.success('Your feedback is saved')}}><label>A short reason (optional)<textarea maxLength={500} value={draft.reason||''} onChange={e=>setDraft({...draft,reason:e.target.value})}/></label><button className="btn primary">Save my feedback</button></form></Modal>
<Modal open={modal==='observation'} onClose={()=>setModal('')} title="Where did you find it?" description="A dated household report. This does not claim current stock."><form className="stack" onSubmit={e=>{e.preventDefault();s.mutate('observation',{...draft,currency,user:s.user.id});setModal('');toast.success('Purchase observation saved')}}><label>Store name and place<input required value={draft.store||''} onChange={e=>setDraft({...draft,store:e.target.value})}/></label><label>Purchase date<input required type="date" max={new Date().toISOString().slice(0,10)} value={draft.date||''} onChange={e=>setDraft({...draft,date:e.target.value})}/></label><label>Observed price ({currency}) · optional<input type="number" min="0" step="0.01" value={draft.price??''} onChange={e=>setDraft({...draft,price:e.target.value})}/></label><button className="btn primary">Save observation</button></form></Modal>
<Modal open={modal==='settings'||modal==='profile'} onClose={()=>setModal('')} title={modal==='settings'?'For the household.':'Just your preferences.'} description="Optional and editable. Missing catalogue data never proves that a product meets a requirement."><form className="stack" onSubmit={async e=>{e.preventDefault();const constraints=(draft.constraintsText||'').split('\n').filter((x:string)=>x.trim()).map((line:string)=>{const [kind,...value]=line.split(':');return{kind:kind.trim(),value:value.join(':').trim()}});if(constraints.some((c:any)=>!['preference','exclusion','allergy','certification'].includes(c.kind)||!c.value))return toast.error('Use one requirement per line, such as allergy: milk');const {name,constraintsText,categoriesText,...rest}=draft;if(modal==='settings'&&categoriesText!==undefined)rest.categories=categoriesText.split(',').map((v:string)=>v.trim()).filter(Boolean);const r=await act(modal==='settings'?'settings':'profile',modal==='settings'?{name,settings:{...rest,constraints}}:{name,preferences:{...rest,constraints}},'Preferences saved');if(r)setModal('')}}><label>{modal==='settings'?'Household name':'Your display name'}<input required maxLength={80} value={draft.name||''} onChange={e=>setDraft({...draft,name:e.target.value})}/></label>{modal==='settings'&&<label>Usual shopping country<Choice label="Usual country" options={countries} value={draft.country||'IT'} onChange={v=>setDraft({...draft,country:v})}/></label>}{modal==='settings'&&<label>Household categories · comma separated<textarea value={draft.categoriesText??(draft.categories||[]).join(', ')} onChange={e=>setDraft({...draft,categoriesText:e.target.value})}/></label>}<label>Language preference<Choice label="Language preference" value={draft.language||'en'} onChange={language=>setDraft({...draft,language})} options={{en:'English',it:'Italiano',fr:'Français',de:'Deutsch',es:'Español'}}/></label><p className="fine">The interface currently uses English. Your preference is saved.</p><label>Preferences and requirements<textarea rows={5} value={draft.constraintsText||''} placeholder={'preference: organic\nexclusion: palm oil\nallergy: milk\ncertification: gluten-free'} onChange={e=>setDraft({...draft,constraintsText:e.target.value})}/></label><p className="fine">One per line: preference, exclusion, allergy or certification, followed by a colon and the ingredient or label. Use English catalogue terms. Free-text matching is conservative and cannot establish allergy safety. Always check the package.</p><button disabled={busy||(modal==='settings'&&!['owner','admin'].includes(household?.role))} className="btn primary">Save preferences</button></form></Modal>
<Modal open={modal==='about'} onClose={()=>setModal('')} title="Familiar things. Clear information." wide><div className="stack prose"><Brand/><p>Same Again keeps shared lists, exact favourites and approved country alternatives together.</p><h3>How candidates are ranked</h3><p>We retrieve a category's products, require a destination-country record, then exclude candidates with missing or conflicting required attributes. Shared ingredients, compatible per-100-unit nutrition, pack size and brand contribute to a reproducible score. The chosen priority changes their weight. Match bands summarise evidence; they are not a percentage of identicalness.</p><p>Intended use is inferred from a shared category. Taste and texture require member feedback. Sweetening, powder and concentrate differences can disqualify candidates. No confident evidence means no confident match.</p><h3>Sources and licences</h3><p>Contains information from <a href="https://world.openfoodfacts.org" target="_blank" rel="noreferrer">Open Food Facts</a>, made available under the <a href="https://opendatacommons.org/licenses/odbl/1-0/" target="_blank" rel="noreferrer">Open Database Licence 1.0</a>. Individual contents are under DbCL 1.0. Product photos are supplied by Open Food Facts contributors under <a href="https://creativecommons.org/licenses/by-sa/3.0/" target="_blank" rel="noreferrer">CC BY-SA 3.0</a>. Original product pages provide source attribution. Catalogue records are cached separately from private household data.</p><h3>Imported retailer records</h3><p>Coop and Migros imports are partial. Indexed page titles are labelled separately from successfully scraped product facts. Each record links its source; no barcode, missing nutrient or branch stock is inferred. Retailer content is not covered by the Open Food Facts licence. Retailer photos and promotional descriptions have not been copied.</p><h3>What we can and cannot know</h3><p>Catalogue entries are community-maintained and may be incomplete or outdated. A country entry does not establish current availability. Nearby stores use OpenStreetMap data via Photon, with map links and no connected inventory. Prices are entered by members, with currency and date. Nothing is automatically purchased.</p><h3>Privacy and syncing</h3><p>Every shared request requires a signed-in household member. Shopping changes sync about every four seconds. Cached active lists work offline after first loading. Conflicting edits stop for review. Account deletion anonymises contributions retained in shared lists; household owners must transfer ownership or delete their household first.</p><p>Camera access happens only when you open the scanner. Location is used only for an explicit place search, rounded before sending to Photon, and never saved as account history. Private photos stay within the household. Sign out clears this device's offline cache.</p><a className="btn" href="/device-check" target="_blank">Check this device <Monitor size={16}/></a><h3>Meals, nutrition & ingredient evidence</h3><p>Shared recipes keep photos, weighed ingredients and methods. Meal plans preserve a recipe snapshot. Nutrition uses matched per-100-g or per-100-ml values, scales by portions, and labels incomplete totals. Generic data is from the Swiss Food Composition Database, FSVO v7.1, reused with attribution under its published dataset permission. Generic composition values are averages and do not establish brand identity or allergy safety.</p><p>Ingredient context currently covers four sweeteners, with links to EFSA and WHO sources. It is not a comprehensive hazard ranking. No concentration or personal exposure is inferred. Targets are optional values entered by you.</p><h3>Current limitations</h3><p>The interface is English; language preferences are saved. Product coverage is strongest for packaged food. Household essentials work as generic or private items. No retailer stock, verified taste equivalence, background reminders or full pantry tracking is connected. Family access also depends on the site's hosting access policy.</p><a className="btn" href="/guide" target="_blank">Setup, testing & technical guide <ExternalLink size={16}/></a></div></Modal>
<Modal open={!!merge} onClose={()=>setMerge(null)} title="Already on your list." description="This is the same product, pack size, unit and note. Would you like to combine quantities?"><p>{merge?.data.name}</p><div className="row wrap"><button className="btn primary" onClick={()=>{s.mutate('item',{...merge.existing.data,quantity:merge.existing.data.quantity+merge.data.quantity},merge.existing);setMerge(null);toast.success('Quantities combined')}}>Combine quantities</button><button className="btn" onClick={()=>{s.mutate('item',merge.data);setMerge(null)}}>Keep separate</button></div></Modal>
<Modal open={!!s.conflict} onClose={()=>{}} title="Two shoppers, one change." description="Someone updated this item while your change was waiting. Review both versions before deciding."><div className="grid2"><div><h3>Your pending change</h3><p>{s.conflict?.op.data.name}</p><p>{s.conflict?.op.data.quantity} {s.conflict?.op.data.unit} · {s.conflict?.op.data.done?'Purchased':'Outstanding'}</p><p>{s.conflict?.op.data.notes}</p>{s.conflict?.op.kind!=='item'&&<pre className="conflict-data">{JSON.stringify(s.conflict?.op.data,null,2)}</pre>}</div><div><h3>Latest shared version</h3><p>{s.conflict?.latest?.data.name}</p><p>{s.conflict?.latest?.data.quantity} {s.conflict?.latest?.data.unit} · {s.conflict?.latest?.data.done?'Purchased':'Outstanding'}</p><p>{s.conflict?.latest?.data.notes}</p>{s.conflict?.op.kind!=='item'&&<pre className="conflict-data">{JSON.stringify(s.conflict?.latest?.data,null,2)}</pre>}</div></div><div className="row wrap"><button className="btn primary" onClick={()=>s.resolve(false)}>Use shared version</button><button className="btn" onClick={()=>s.resolve(true)}>Apply my change after review</button></div></Modal>
<AlertDialog open={!!confirm} onOpenChange={v=>!v&&setConfirm(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>{confirm?.title}</AlertDialogTitle><AlertDialogDescription>{confirm?.description}</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={()=>{void confirm?.run();setConfirm(null)}}>Confirm</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog>
</SidebarProvider>}
